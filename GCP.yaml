apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  annotations:
    client.knative.dev/user-image: gcr.io/cloudrun/placeholder
    run.googleapis.com/ingress: all
    run.googleapis.com/ingress-status: all
  labels:
    cloud.googleapis.com/location: asia-northeast1
    gcb-trigger-id: 1a487de7-1a17-4b92-8330-2822df8e3d1c
    managed-by: gcp-cloud-build-deploy-cloud-run
  name: sou-web-net
  namespace: '756030835970'
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: '100'
        run.googleapis.com/client-name: cloud-console
      name: sou-web-net-00006-kag
    spec:
      containerConcurrency: 80
      containers:
        - env:
            CONTENTFUL_SPACE_ID: ${{ secrets.CONTENTFUL_SPACE_ID }}
            CONTENTFUL_DELIVERY_TOKEN: ${{ secrets.CONTENTFUL_DELIVERY_TOKEN }}
            MAIL_USER: ${{ secrets.MAIL_USER }}
            MAIL_PASS: ${{ secrets.MAIL_PASS }}
            MAIL_TO: ${{ secrets.MAIL_TO }}
            NEXT_PUBLIC_GA_ID: ${{ secrets.NEXT_PUBLIC_GA_ID }}
          image: gcr.io/cloudrun/placeholder
          ports:
            - containerPort: 3000
              name: http1
          resources:
            limits:
              cpu: 1000m
              memory: 128Mi
      serviceAccountName: 756030835970-compute@developer.gserviceaccount.com
      timeoutSeconds: 10
  traffic:
    - latestRevision: true
      percent: 100
